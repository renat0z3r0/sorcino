[tool.pytest.ini_options]
# Make the project root importable so `import sorcino_cli`, `checks.*`, etc. resolve
# without installing the package.
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
